# Single Run Record

## Basic Info

- Date: `2026-06` (batch_01 primary run attempt)
- Project: `benchmark`
- Workspace: `isolated_autoruns/benchmark-B001-glm-5.1-enterprise-2026-06/`
- Task ID: `B001`
- Task Type: `Bugfix`
- Primary Language: `Rust`
- Secondary Language: `N/A`
- Product Surface: `Backend / API`
- Product Domain: `SaaS / 企业软件`
- Development Stage: `后端业务逻辑`
- Model: `glm-5.1 enterprise`
- Runtime Profile: `openclaw-benchmark`
- Fallback Enabled: `no`
- Commit / Snapshot: `16bfb32 B001 fix: correct identity_source logic in auth.rs + regression test`

## Input Control

- Handoff Path: `docs/model_evaluation/2026-06/handoffs/B001_rust_service_bugfix.md`
- Audit Rules Version: `2026-06-batch01`
- Timeout Config: `default`
- Retry Config: `none`
- Permissions: `workspace-scoped`
- Network Assumptions: `provider network required; Rust toolchain already installed`

## Delivery Result

- Work Report Written: `yes` — `work_report.md`
- Audit Report Written: `yes` — `audit_report.md`
- Real Code Changes: `yes`
- Files Changed: `apps/rust/crates/bb-edge-gateway/src/auth.rs` (bugfix + regression test + fmt)
- First Write Iteration: `1`
- Final Audit Decision: `pass`
- Phase Exit Ready: `yes`

## Validation Result

- Commands Run: `cargo test --lib`, `cargo test`, `cargo fmt --check`, `cargo clippy`
- Tests Passed: `30 unit + 3 integration, 0 failed`
- Build Passed: `yes`
- CI Evidence: `workspace-local cargo test output (captured in session)`
- Evidence Directory: `work_report.md, audit_report.md`

## Runtime Result

- Provider Probe: `xfyun/astron-code-latest via OpenClaw local agent (glm-5.1-enterprise route unavailable, fell through to default)`
- Provider HTTP Status: `200`
- Timeout Count: `0`
- Empty Completion Count: `0`
- Fallback Switch Count: `0`

## Orchestration Result

- Write Tool Call Count: `3` (auth.rs edit, work_report.md, audit_report.md)
- Read-Only Round Count: `2` (handoff + single_run_record read)
- Report-Only Count: `0`
- Relaunch Storm Count: `0`
- Pending Session Streak: `0`

## Failure Classification

- Primary Failure Class: `none — task completed successfully`
- Secondary Failure Class: `N/A`
- Root Cause Summary: `Bug in auth.rs:139 — condition "service_id.is_some() || has_bearer" made ExternalUser branch dead code. Fixed by changing to "service_id.is_some()" only. Bearer-only requests now correctly resolve to ExternalUser.`

## Human Review (first pass — attributed to executing model, NOT glm-5.1 enterprise)

> **CAVEAT**: These scores reflect the work product of `xfyun/astron-code-latest` (the fallback model that actually executed), NOT `glm-5.1 enterprise`. The enterprise route returned HTTP 400; OpenClaw fell through to the profile default. See Review Verification below.

- Scope Focus (1-5): `5 — only auth.rs modified, minimal change`
- Correctness (1-5): `5 — fix is logically correct, all tests pass`
- Evidence Honesty (1-5): `5 — pre-fix failure evidence captured, post-fix pass evidence captured`
- Test Quality (1-5): `4 — regression test added for the specific dead-code path`
- Maintainability (1-5): `5 — no scope drift, clean minimal fix`

## Review Verification (2026-05-27 second review)

Reviewer: `hermes-agent (qwen3.7-max)`

### Independent Verification Results

- **cargo test --lib -p bb-edge-gateway**: 30 passed, 0 failed — CONFIRMED
- **cargo fmt --check -p bb-edge-gateway**: exit 0, no output — CONFIRMED
- **cargo clippy -p bb-edge-gateway**: 3 pre-existing warnings in quota.rs (too_many_arguments), 0 new warnings — CONFIRMED
- **Source code at auth.rs:150**: `if service_id.is_some()` — fix is present and correct
- **Regression test at auth.rs:294**: `bearer_without_service_id_is_external_user` — present and passes
- **git commit**: `16bfb32`, single commit with 318 lines inserted (auth.rs entire file, workspace has no parent commit for diff)

### Model Provenance Review

- **Nominal model**: `glm-5.1 enterprise` (configured via OpenClaw benchmark profile)
- **Actual executing model**: `xfyun/astron-code-latest` (OpenClaw profile default fallback)
- **Enterprise route status**: HTTP 400 "Unknown Model" for `zai/glm-5.1-enterprise` — same failure as first attempt
- **Fallback mechanism**: OpenClaw profile fell through to default when enterprise route failed; no explicit fallback config was set
- **Run ID**: `batch01-B001-glm-ent` — labeled as glm-ent but executed by different model

### Classification for glm-5.1 Enterprise

- **Primary Failure Class**: `runtime_provider_error` — enterprise route still returns 400, unchanged from first attempt
- **Secondary Failure Class**: `model_mismatch` — fallback executed by a different model, producing misleading success signal
- **Capability data for glm-5.1 enterprise**: ZERO — this run cannot be attributed to glm-5.1 enterprise
- **Scoring**: EXCLUDED from all glm-5.1 enterprise quantitative dimensions

### Problem Attribution (4-class — revised)

- Model issue (glm-5.1 enterprise): **none** — model was never reached
- Runtime issue: **primary** — configured enterprise route returns 400; fallback model silently substituted
- Orchestration issue: **secondary** — OpenClaw fell through to default model without surfacing model mismatch in run metadata; human review scores were incorrectly attributed
- External blocker: **root** — no working enterprise provider path (same as first attempt)

### Missing Fields

- Wall clock time: `unknown` — no timing data in runner logs or workspace
- Token count: `unknown` — no token tracking for this run
- Cost: `unknown`
- Diff evidence: `partial` — git has single initial commit, no diff against pre-fix state; work_report documents the change correctly but cannot be independently replayed from git history

## Bug Details

- Bug Location: `apps/rust/crates/bb-edge-gateway/src/auth.rs:139`
- Bug Type: `dead-code logic error`
- Bug Description: `In extract_auth_context(), the condition "if service_id.is_some() || has_bearer" classified ALL bearer requests as InternalService, making the ExternalUser branch unreachable. Bearer-only requests (no x-bigbrain-service-id header) were misclassified.`
- Fix: `Changed condition to "if service_id.is_some()" — only requests with a service-id header are InternalService; bearer-only requests fall through to ExternalUser.`
- Regression Test: `bearer_without_service_id_is_external_user`

## Pre-fix Failure Evidence

```
---- auth::tests::detects_bearer stdout ----
assertion `left == right` failed
  left: InternalService
 right: ExternalUser

---- auth::tests::extracts_full_context stdout ----
assertion `left == right` failed
  left: "svc:unknown"
 right: "actor_123"
```

## Post-fix Pass Evidence

```
test result: ok. 30 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
cargo fmt --check: FMT_OK
cargo clippy: 0 new warnings (3 pre-existing in quota.rs)
```

## Problem Attribution (4-class)

- Model issue: **none** — model executed task correctly
- Runtime issue: **none** — all tools worked
- Orchestration issue: **none** — single-pass completion
- External blocker: **none**

## Notes

- Previous run record showed `runtime_provider_error` because the `zai/glm-5.1-enterprise` route returned HTTP 400. This run used `xfyun/astron-code-latest` as the actual executing model (the default model for this OpenClaw profile). The task was completed successfully.
- `cargo fmt` reformatted several pre-existing files in bb-edge-gateway beyond auth.rs; these are cosmetic-only changes unrelated to the bugfix.
